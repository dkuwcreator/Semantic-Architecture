{
  "servers": {
    "semantic-architecture": {
      "command": "docker",
      "args": [
        "run",
        "--rm",
        "-i",
        "-v",
        "${workspaceFolder}:/workspace",
        "-e",
        "REPO_ROOT=/workspace",
        "semantic-architecture-mcp-server"
      ]
    }
  }
}

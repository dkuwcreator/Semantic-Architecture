version: '3.8'

services:
  mcp-server:
    build:
      context: .
      dockerfile: mcp-server/Dockerfile
    command: ["--http"]
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
    restart: unless-stopped
    container_name: semantic-architecture-mcp
